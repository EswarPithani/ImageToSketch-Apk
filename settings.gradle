pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':opencv'
project(':opencv').projectDir = new File('C://Users//HP//AndroidStudioProjects//Image2Sketch//openCV/sdk/java')


dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // If you're using any dependencies from JitPack
    }
}

rootProject.name = "Image2Sketch"
include ':app'
include ':openCV'
